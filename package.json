{
  "name": "live-tracking-app",
  "version": "1.0.0",
  "private": true,
  "description": "Monorepo for live location tracking system",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "start": "turbo run start"
  },
  "devDependencies": {
    "turbo": "^1.10.0"
  },
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
    "shared": "link:../../../../Library/pnpm/global/5/node_modules/@live-tracking/shared"
  }
}
